# 단체 PCA 플랫폼

이 파일은 Claude가 매 세션 시작할 때 읽는 프로젝트 설명서다.
새 결정이 생기면 여기에 추가한다.

---

## 무엇을 만드는가

대학 공과계열 학과에 파는 진로 지표 검사 플랫폼.

학생이 검사를 보면 **그 전공으로 갈 수 있는 직무 분야**를 알려주고,
그 직무에 필요한 **역량**과 채우기 위해 들어야 할 **과목**까지 제시한다.

- 응시 대상: 대학 재학생
- 계약 주체: 학과 (또는 취업지원처)
- 첫 대상 학과: 기계공학과
- 참고 모델: 르네상스러닝 (검사 → 리포트 → 처방 구조가 동일)

---

## 확정된 결정

| 항목 | 결정 |
|---|---|
| 계정 발급 | 계약 후 관리자가 발급. 셀프 가입 아님 |
| 계정 단위 | 기관 단위 + 개인 단위 둘 다 (개인은 나중) |
| 과금 | 계약 좌석(응시권) 차감. 구독/크레딧 아님 |
| 출제 방식 | 고정 문항. 적응형(CAT) 아님 |
| 중간 저장 | 문항마다 즉시 저장, 이어보기 지원 |
| 응시 자격 | 학과가 올린 명단 기반 |
| 결과 공개 | 학과 담당자 승인 후 공개 |
| 도메인 | 국가별 별도 도메인 |
| 다국어 | translations 테이블에 행으로 저장 |
| 개발 순서 | 계정 발급형 먼저. **셀프 가입·개인 결제는 1차 범위로 올림** (2026-09-10) |
| 소개 사이트 | 나라별로 만든다. 글로벌(영어)·한국 먼저, 카자흐스탄·터키는 다음 |
| 나누는 축 | **학교급이 아니라 전공.** 기계 → 전기전자 → 컴퓨터 순 (2026-09-10) |
| 고교 대응 | **트랙 하나(`HS`)로 합친다.** 일반계고·마이스터고를 나누지 않고 `goal`(진학형/취업형) 값으로만 갈린다 |
| 검사지 개수 | **두 벌.** 고교판 `HS_V1` · 대학판 `UNIV_V1`. 트랙 4개는 채점 가중치 행일 뿐 |
| 문항 출처 | **기존 PCA 문항 재사용.** 한 글자도 고치지 않고 `questions` 에 그대로 넣고 `axis_code` 만 매단다. 공학 활동 8축 48문항만 신규 (2026-09-11) |
| 고객 | **B2B(학과)와 B2C(개인) 동시.** 학과 계약이 곧 B2C 획득 채널 |
| 1차 국가 | **한국만.** 미국은 설계만 해두고 만들지 않는다 (공고 원가가 열 배) |
| 브랜드 | **METRI.** PCA 는 검사(진단 엔진) 이름으로 남긴다 (2026-09-11) |
| 진입 구조 | **개인 · 학교 두 개의 문.** 기업 채널은 뺐다 (2026-09-11) |
| 소개 사이트 | 원고 네 벌 — `global` · `kr` · `kz` · `tr`. kz·tr 은 현지 검수 전 |
| 기업 채널 | **만들지 않는다.** 법(등록·허가)과 순서(학생 1,000명) 둘 다 안 맞는다 (2026-09-11) |

---

## 법적 범위 (2026-09-11)

보유: **직업정보제공사업 신고 J1700020220007호**. 유료직업소개사업 등록은 없다.

- **정보 제공은 되고, 알선은 안 된다.** 공고를 띄우고 학생이 직접 지원하는 데까지다
- 직업정보제공사업자 준수사항(시행령 제28조)이 금지하는 것 —
  **구직자 이력서 발송 대행**, **취업추천서 발부**, 신원 미확인 구인광고 게재,
  광고에 사업자 연락처 기재. 최저임금 미달·성매매업소 광고 게재 금지
- 그래서 **"추천 기업" 이라는 말을 쓰지 않고** "적합도가 높은 기업" 으로 쓴다
- **채용 성공 수수료는 등록 전에는 받지 않는다**
- 1차 MVP 매출(학과 B2B + 개인 B2C)은 지금 신고만으로 전부 가능하다

**터키** — İŞKUR 면제는 "지원 버튼·회원제·등록 절차가 전혀 없는" 사이트만 받는다.
METRI 는 로그인이 있어 면제가 안 된다. 담보만 2026년 660,600 TL 이다.
→ **공고 개별 노출을 끄고 집계 통계만 쓴다.**

**카자흐스탄** — ЧАЗ 면허제가 도입되는 중이다. → **알선 쪽은 아예 건드리지 않는다.**

자세한 것과 출처는 `docs/metri/10_legal_kr.md`. **법률 자문이 아니므로 노무사·행정사 확인을 거친다.**

---

## 설계 원칙 (어기면 안 됨)

**1. 로그인 주체는 users 하나다.**
기관 소속 학생이든 개인회원이든 전부 `users`에 들어간다.
소속은 `memberships`로 연결한다. 회원 테이블을 기관용/개인용으로 나누지 않는다.

**2. 사람이 읽는 이름은 전부 translations에 모은다.**
`name_ko`, `name_en` 같은 컬럼을 만들지 않는다.
나라를 추가할 때 컬럼이 아니라 행이 늘어나야 한다.

**3. 채점 산식은 코드가 아니라 데이터다.**
직무 적합도는 `scoring_weights` 테이블의 가중치로 계산한다.
산식을 코드에 하드코딩하면 학과별·국가별로 다르게 줄 수 없고, 바꿀 때마다 배포해야 한다.

**4. 검사 문항은 수정하지 않고 버전을 올린다.**
`instruments.version`. 작년 응시자의 결과가 올해 문항 수정으로 바뀌면 안 된다.

**5. 마케팅 사이트와 검사 플랫폼은 별개다.**
국가별 도메인은 소개·영업용. 학생이 검사를 보고 결과를 받는 플랫폼은 전 세계 하나다.
그래야 국가 간 비교가 가능하고, 그게 이 서비스의 핵심 자산이다.

**6. 점수는 산식이 만들고, 문장은 모델이 쓴다.** (2026-09-10 추가)
LLM 이 만든 값은 `job_fit_scores` 에 절대 들어가지 않는다.
같은 입력이면 항상 같은 점수가 나와야 하고, 학과 담당자가 재계산해 볼 수 있어야 한다.
설명 못 하는 점수는 팔리지 않는다.

---

## 데이터 레이어

**전 세계 공통** (나라 바뀌어도 재사용)
majors, job_clusters, competencies, job_competency_map, 검사 문항

**국가·대학별** (진출할 때마다 새로 채움)
organizations, courses, course_competency_map, translations

---

## 화면 목록 (MVP)

**운영사 관리자**
1. 기관 목록
2. 계약·응시권 등록
3. 검사 문항 관리
4. 매핑 데이터 관리
5. 전체 응시 현황

**학과 담당자**
6. 회차 생성
7. 명단 업로드 및 계정 일괄 발급
8. 응시 진행 현황
9. 단체 리포트 → `mockups/03_group_report.html`

**학생**
10. 로그인
11. 검사 응시 → `mockups/01_test_screen.html`
12. 결과지 → `mockups/02_student_report.html`

소개 사이트는 만든다. (2026-09-04 결정 변경. 그전에는 MVP에서 뺐었다.)
영업은 여전히 대면으로 하되, 나라별 도메인에 소개 사이트를 두고 문의를 받는다.
검사 플랫폼과는 코드가 분리돼 있다 — `marketing/` 이 소개 사이트고, `src/` 가 플랫폼이다.

소개 사이트는 한 코드베이스에서 나라별 원고만 갈아 끼운다.

```
SITE=global npm run build   →  영어판
SITE=kr     npm run build   →  한국어판
SITE=kz / SITE=tr           →  원고 파일만 추가하면 된다
```

각 사이트의 로그인 버튼은 전부 같은 플랫폼 한 곳을 가리킨다 (설계 원칙 5).

---

## 개발 순서

1. **계정** — users, organizations, memberships, 로그인, 비밀번호 재설정, 관리자에서 기관 생성
2. **계약과 회차** — contracts, seats, test_sessions, 명단 엑셀 업로드로 계정 일괄 생성
3. **응시** — 응시 화면, responses 저장, 이어보기
4. **채점과 결과** — scoring_weights 적용, 결과지, 단체 리포트

3단계까지는 채점 산식 없이 진행 가능하다.

---

## 기술 스택

- Next.js (App Router) + TypeScript
- PostgreSQL
- 인증은 직접 구현하지 말고 검증된 라이브러리 사용
- 파일 저장은 S3 호환 스토리지
- 브라우저 저장소(localStorage 등)에 응답을 보관하지 않는다. 서버에 저장한다.

---

## 파일 안내

- `db/schema.sql` — 테이블 정의. 여기부터 읽을 것
- `mockups/*.html` — 확정된 화면 시안. 디자인 토큰과 레이아웃을 여기서 가져올 것
- `docs/pca_mapping_template.xlsx` — 매핑 데이터 입력 양식
- `src/` — 검사 플랫폼 (Next.js). 로그인·응시·결과
- `marketing/` — 나라별 소개 사이트. 원고는 `marketing/src/content/*.ts`

**METRI 확장 (2026-09-10 추가)**

- `docs/metri/README.md` — 설계 문서 10편. 여기부터 읽을 것
- `db/schema_metri.sql` — 확장 스키마. `db/schema.sql` 다음에 적용한다
- `data/metri/*.json` — **3개 전공 Skill Tree 원본. 고칠 곳은 여기다**
- `db/seed/metri/skill_tree.sql` · `docs/metri/generated/skill_tree.md` — 자동 생성. 직접 고치지 말 것
- `prototypes/metri/index.html` — 화면 프리뷰 (전 화면을 눌러 볼 수 있다)

```bash
npm run metri:build     # JSON → 시드 SQL + 문서 표 + 프리뷰 데이터
npm run metri:seed      # 확장 스키마와 시드를 DB 에 적용
```

---

## 앵커사업 (2026, 교육부)

한국판 소개 사이트의 대학 대상 논지는 이 정책에 맞춘다.

- 2025년 출범한 **라이즈(RISE, 지역혁신중심 대학지원체계)** 가 2026년
  **‘지역성장 인재양성체계(앵커)’** 로 명칭·체계가 재정립됐다
- 목표는 ‘지역인재육성-취·창업-정주’ 선순환 생태계와 5극3특 균형성장
- 2026년 예산 21,403억 원(’25년 19,410억). 이 중 **약 4,000억 원을 성과평가
  인센티브 재원**으로 써 지방정부별로 차등 지원한다. 초광역 단위 사업 2,000억 원
- 평가는 ① 예산 나눠먹기가 없었는지 ② 지방정부-대학 소통 ③ **학생·인재에 대한
  충분한 고려가 있었는지** 를 집중 점검한다. 17개 지방정부 평가 결과는 공개된다
- 시도 중점 사항이 ‘대학 선정·사업비 교부’ 에서 **‘학생 중심 지원 강화’** 로 옮겨갔다
- 성과지표 예시에 **지역정주 취업률(졸업 후 해당 시도 내 취업자 비율)** 과
  **지역 내 유지취업률** 이 있다. 교육부 평가 전 지방정부는 대학 대상 자체평가를 한다

**PCA 가 댈 수 있는 것과 없는 것을 섞지 않는다.**
지역정주 취업률은 졸업 후 취업 통계로 확정되는 지표이므로 PCA 가 산출한다고 쓰지 않는다.
PCA 가 대는 것은 학생 체감의 증거(개인 15–20페이지 전략서)와 정주로 가는 과정
데이터(지역기업 매칭률·정주 관심)다.

출처: 교육부 「지역성장 인재양성체계(앵커) 추진방안」 보도자료 (2026.4.2) 및 별첨 추진방안

---

## 확인된 사실 (2026-09-04, 제안서 + academix.co.kr/59)

소개 사이트 원고는 **academix.co.kr 의 PCA 페이지**를 1차 기준으로 한다.
제안서와 어긋나는 곳은 실제 페이지를 따른다.

| 항목 | 실제 페이지 기준 |
|---|---|
| 업무 성향 | 독립형 · **협력형** · 도전형 · 안정형 · 속도중시형 · 품질중시형 (제안서의 "협업형"은 오기) |
| 직무 영역 | **10개** — 경영·전략, 인사·조직, 총무·경영지원, 재무·회계, 금융·투자, 마케팅·홍보·영업, 물류·유통·구매, 생산·운영관리, IT·데이터·서비스기획, 컨설팅·리서치·조사분석 |
| 대상 | 전 학과. 경영학과부터 공학·자연·인문·예체능까지 |
| 결과지 | 00-1, 00-2, 01~09 열한 개 섹션. 탭으로 넘겨보는 구성 |
| 지역 연계 | 대전 예시 — 소재 기관·기업 184곳을 적합도 높음 144 · 보통 40 · 낮음 0 으로 구분. 지역특화 연계 101곳 |
| 저작권 | C-2025-058658 / C-2025-059731 / C-2025-059732 (한국저작권위원회) |
| 준거 체계 | NCS · O*NET 계열 · RIASEC 계열 · NACE · OECD · AERA/APA/NCME |
| 정체성 | 어두운 바탕 + 금색 강조. 결과지만 흰 문서로 띄운다 |
| 전개 국가 | 진행 — 독일·미국·일본·중국·터키·한국 / 예정 — 카자흐스탄·프랑스·남아공·필리핀 |

## 제안서에서만 확인된 사실 (ACADEMIX PCA 제안서)

소개 사이트 원고는 이 제안서를 따른다. 아래는 그전까지 이 파일에 없던 내용이다.

| 항목 | 내용 |
|---|---|
| 개발 주체 | ACADEMIX (대학·공공기관 교육 프로그램 설계·운영) |
| 브랜드 | ACADEMIX (주력) · career peak (STEM 커리어·취업) · JOBINDUSTRY (STEM 석·박사) |
| PCA | Personalized Career Analysis |
| 분석 축 | 적합한 직무 영역 · 업무 성향 · 실행 전략 |
| 업무 성향 | 독립 · 도전 · 속도중시 · 협업 · 안정 · 품질중시 (육각형) |
| 리포트 | 15–20페이지, 10개 섹션(01 핵심 요약 … 09 지역 연계) |
| 핵심 차별점 | 지역 연계·정주형. 대학 소재지 실기업 매칭 (대전 예시 144곳), 적합도 티어 |
| 기관 가치 | 익명 집계 정주 성과 리포트. 대학·RISE 실적 근거. 관리자 대시보드 예정 |
| 도입 | 전용 링크 발급 → 학생 응시 → 성과 자동 집계 |
| 대상 | 학부생 ~ 석·박사 졸업생. STEM 특화 |
| 개발 근거 | 전공자 2,346명 · 설문 2,091명 · 채용공고 428건 · 직무기술서 137건 · NCS 62건 · 현직자 48명 |

**플랫폼 스펙과 어긋나는 부분이 있다.** 이 파일의 개발 순서와 스키마는
"역량 갭 → 개설 과목 처방"을 전제하는데, 제안서에는 그 개념이 없고
"자격·프로젝트·포트폴리오·면접·창업·지역기업"으로 이어진다.
응시 자격도 이 파일은 명단 기반인데 제안서는 전용 링크 발급이다.
소개 사이트는 제안서를 따랐고, 플랫폼 스펙은 그대로 두었다.

**2026-09-10 — 정했다. 셋 다 "하나를 고른다" 가 아니라 "데이터로 흡수한다" 로 푼다.**

| 항목 | 결정 |
|---|---|
| 처방 | **둘 다 낸다.** `skill_gap_items.action_kind` 가 `course` 든 `cert` 든 `ncs_unit` 이든 담는다. 트랙별로 기본 처방 종류만 다르다 |
| 응시 자격 | **둘 다.** 명단 업로드는 B2B, 전용 링크는 B2C·소규모 계약. `seats` 하나로 처리된다 |
| 직무 영역 | **공존한다.** 공통 10개는 성향 축(기존 PCA), 전공별 8개는 METRI 축. 결과지에서 층이 다르다 |

근거는 `docs/metri/07_mvp.md` 마지막 절.

---

## 아직 정해지지 않은 것

| 항목 | 상태 (2026-09-10) |
|---|---|
| 채점 산식 | **제안이 나왔다** — `docs/metri/04_matching.md`. `Fit = 100×(wA·A + wS·S + wP·P + wC·C)`. 계수는 전문가 설정값이고 **100명 검증 전까지는 추정치다** |
| 역량 보유 수준 판정 | **제안이 나왔다** — 증거 모델. 과목·자격·프로젝트에 배점과 신뢰도를 매겨 합산하고 사다리로 레벨 변환 |
| 문항 수와 내용 | **기존 PCA 문항을 쓴다** (2026-09-11). 새로 쓰지 않는다. 저작권 등록분이고 개발 근거(2,346명·2,091 설문)가 이미 있다. 다만 **공학 활동 8축이 비어 있어 48문항만 추가**한다 — `docs/metri/03_assessment.md` 0장·2장 |
| 가격 | **제안이 나왔다** — `docs/metri/08_revenue.md`. 학과 500명 1,400만 / 개인 리포트 29,000 |
| 도메인 | 미정 |

**위 네 개는 확정이 아니라 제안이다.** 숫자마다 근거를 붙여 뒀으니, 동의하지 않으면
그 근거를 보고 바꾸면 된다. 근거 없이 바꾸지는 말 것.
