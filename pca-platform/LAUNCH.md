# 런칭 점검표

오늘 켤 수 있는지를 판단하려고 만든 문서다. **소프트웨어는 준비돼 있고,
막는 것은 대부분 우리 밖에 있다** — 심사·신고·사람이 정해야 하는 문구다.

마지막 점검: 2026-09-16

---

## 1. 코드 — 됐다

| 항목 | 상태 | 확인 방법 |
|---|---|---|
| 스키마가 빈 DB 에서 끝까지 올라감 | ✅ | `db/schema.sql` → `db/schema_metri.sql` → 시드 |
| 문항 253개 적재 (250 + 성실도 3) | ✅ | `npm run metri:items` |
| 개인: 가입 → 결제 → 응시 → 채점 → 결과지 | ✅ | `npm run metri:check` |
| 증거 → 보유 레벨 | ✅ | `npm run metri:evidence` |
| 결제: 국내카드·해외카드·멱등·금액조작 거절 | ✅ | `npm run metri:pay` |
| 학과: 계약 → 회차 → 명단 → 공개 → 단체 리포트 | ✅ | `npm run metri:org` |
| 개인정보 익명화 | ✅ | `npm run metri:erase` |
| **커리어메트리 플러스** 고교 문항 115개 적재 | ✅ | `npm run metri:items:hs` |
| 커리어메트리 플러스: 결제 → 응시 → 채점 → 결과지 | ✅ | `npm run metri:hs` |
| 고교학점제 과목표 74개 (2022 개정) | ✅ | `npm run metri:subjects` |
| 과목 처방 — 학년별·선수과목·대학 권장 | ✅ | `npm run metri:prescribe` |
| 현장 → 대학 → 고교 → 중학 사슬 (직무 16 · 요구 48) | ✅ | `npm run metri:subjects` |
| **무료·유료 경계** — 무료 응시에 유료 데이터가 오지 않음 | ✅ | `npm run metri:tier` |
| **환불선** — 상품마다 다른 '제공 개시' 를 가름 | ✅ | `npm run metri:refund` |
| **응시권 코드** — 코드가 좌석이 되고 한 번만 된다 | ✅ | `npm run metri:redeem` |
| **알림 대기열** — 알림이 결제를 막지 않는다 | ✅ | `npm run metri:outbox` |
| **컨테이너 한 장** — 빌드·비루트·헬스체크 | ✅ | `Dockerfile` · `docker-compose.yml` |
| 타입체크 · 프로덕션 빌드 (플랫폼) | ✅ | `npm run typecheck && npm run build` |
| 소개 사이트 4개국 빌드 | ✅ | `cd marketing && SITE=kr npm run build` (global·kz·tr 도) |
| 한국어·영어·튀르키예어 전 화면 | ✅ | 브라우저로 확인 |

---

## 2. 오늘 켜려면 반드시 있어야 하는 것 — 아직 없다

### 2-1. 결제를 받으려면

| 막는 것 | 누가 | 걸리는 시간 |
|---|---|---|
| **PG 가맹점 심사** | PortOne + 카드사 | 영업일 3~10일. 오늘 안 됨 |
| **통신판매업 신고** | 관할 구청 | 3~5일. 사업자등록증·구매안전서비스 확인증 필요 |
| **해외카드 채널 계약** | PortOne | 국내 채널과 별도 심사 |

심사가 끝나면 코드에서 할 일은 `.env` 다섯 줄뿐이다.

```
PAYMENTS_PROVIDER=portone
PORTONE_STORE_ID=...
PORTONE_CHANNEL_KEY=...            # 국내
PORTONE_CHANNEL_KEY_GLOBAL=...     # 해외 Visa·Mastercard
PORTONE_API_SECRET=...
PORTONE_WEBHOOK_SECRET=...
```

> **`PAYMENTS_PROVIDER=mock` 인 채로 운영에 올리면 서버가 뜨지 않는다.**
> 돈을 안 받고 좌석이 나가는 사고는 코드가 거절한다.

### 2-2. 사업자 정보 — 지어낼 수 없어서 비워 뒀다

전자상거래법 제10조가 "소비자가 알아보기 쉽게" 표시하라고 정한 것들이다.
`marketing/src/content/kr.ts` 의 `legal` 절을 채우면 푸터에 바로 나온다.
**비어 있으면 푸터에 금색 "확인 필요" 로 찍힌다** — 빈칸으로 두면 그대로
런칭되기 때문에 일부러 눈에 띄게 했다.

- 상호 · 대표자 성명
- 영업소 주소 · 전화 · 이메일
- 사업자등록번호
- 통신판매업 신고번호
- 개인정보 보호책임자

직업정보제공사업 신고번호(J1700020220007)만 이미 들어 있다.

### 2-3. 문서 검토

`/terms` · `/privacy` · `/refund` 세 화면을 만들어 뒀다. 내용은 **코드가
실제로 하는 일에 맞춰** 썼다 — 보유기간, 파기 절차, 환불선(응시 시작 전/후),
5명 미만 칸 차단까지 전부 구현과 일치한다.

**그래도 법률 자문이 아니다.** 게시 전에 변호사 또는 개인정보 전문가의
검토를 받아야 한다. 각 화면 위에 그 경고가 박혀 있다.

---

## 3. 운영 환경

| 항목 | 상태 | 비고 |
|---|---|---|
| PostgreSQL | 미정 | 관리형(RDS·Supabase·Neon) 권장. 백업·시점복구 켤 것 |
| 배포 | **코드는 됐다** | `docker compose up -d` 한 줄. `docs/metri/23_operations.md` |
| 밤 당번 | **됐다** | `tick` 컨테이너가 5분마다 `/api/ops/tick` 을 두드린다 |
| 아침 브리핑 | **됐다** | `npm run metri:ops` — 막힌 것과 사람이 할 일이 한 장 |
| 도메인 | **이름 확정, 미구매** | `careermetri.co.kr`(한국 소개) · `careermetri.com`(글로벌) · `app.careermetri.com`(플랫폼). DNS 는 조용하지만 등록 여부는 대행자에서 확인하고 산다 |
| HTTPS | 필수 | 쿠키가 세션이므로 없으면 안 된다 |
| `AUTH_SECRET` | 필수 | `openssl rand -base64 32`. 개발용 값을 그대로 쓰지 말 것 |
| 웹훅 주소 | 필수 | PortOne 콘솔에 `https://.../api/payments/webhook` 등록 |
| `OPS_TOKEN` | 필수 | `openssl rand -hex 24`. 16자 미만이면 `/api/ops/tick` 이 닫힌다 |
| `MAIL_*` | 없어도 뜬다 | 비면 알림이 쌓이기만 한다. 채우면 순서대로 나간다 |

---

## 4. 켠 다음에 바로 해야 하는 것

1. **첫 결제를 실제 카드로 한 번 해 본다.** 테스트 모드와 운영은 다른 길이다.
2. **웹훅이 들어오는지 본다.** `payment_events` 에 행이 쌓이는지.
3. **결과지를 한 장 인쇄해 본다.** A4 로 뽑아 잘리는 곳이 없는지.
4. **첫 100명 검증을 건다.** 채점 산식의 계수와 레벨 사다리는 아직 추정치다
   (`docs/metri/04_matching.md`). 100명이 쌓이면 그 값을 실측으로 바꾼다.

---

## 5. 아직 안 만든 것 (런칭을 막지는 않는다)

- 운영사 관리자 화면 일부 (기관 목록·전체 응시 현황)
- 학과 단체 리포트 PDF 내보내기
- 전공 확장 — 지금 문항은 **기계공학과 한 벌**이다. 전기전자·컴퓨터는
  Skill Tree 만 있고 문항이 없다
- 보유기간 만료분 자동 파기 (지금은 본인 탈퇴만. 5년 지난 주문 정리는 수동)
- 환불 송금 — 판정은 `refundable()` 이 하고 PG 취소 호출만 사람이 한다

---

## 결론

**오늘 켤 수 있는 것** — 소개 사이트 네 나라 판, 그리고 학과·학교 계약 기반
플랫폼(결제 없이 좌석을 계약으로 발급하는 경로)은 오늘 올려도 된다.
**커리어메트리 플러스의 학교 학년 단위 계약이 여기에 들어간다** — 계약 좌석은
`reportLevel` 이 결제 기록 없이 `full` 로 읽으므로 학생에게 사슬과 과목
처방까지 전부 열린다.

**밤에 사람이 없어도 되는가** — 그렇다. 결제·코드 교환·채점·결과지 공개가
승인을 기다리지 않고, 알림은 대기열로 빠져 있어 메일 서버가 죽어도 결제가
확정된다. 사람이 아침에 하는 일은 브리핑 한 장을 읽고 거기 적힌 것만
하는 것이다 — 코드 찍기, 승인제 회차 공개, 환불 송금.

**오늘 켤 수 없는 것** — 개인 결제(B2C). PG 심사와 통신판매업 신고가
끝나야 한다. 코드는 기다리고 있고, 바꿀 것은 `.env` 다섯 줄이다.
커리어메트리 플러스의 **무료 구간(계열 적합까지)은 0원 주문이라 PG 를 거치지
않지만**, 유료 구간으로 올리는 19,000원 결제는 같은 심사를 기다린다.
