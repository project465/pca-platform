{
  "name": "pca-marketing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3100",
    "build": "next build",
    "start": "echo '정적으로 내보냅니다. npm run build:<판> 뒤 npm run serve 를 쓰세요' && exit 1",
    "typecheck": "tsc --noEmit",
    "serve": "node scripts/serve-static.mjs out 3100",
    "build:global": "SITE=global next build && rm -rf out-global && mv out out-global",
    "build:kr": "SITE=kr next build && rm -rf out-kr && mv out out-kr",
    "build:kz": "SITE=kz next build && rm -rf out-kz && mv out out-kz",
    "build:all": "npm run build:global && npm run build:kr && npm run build:kz"
  },
  "dependencies": {
    "d3-geo": "^3.1.1",
    "next": "15.5.25",
    "pretendard": "1.3.9",
    "react": "19.1.1",
    "react-dom": "19.1.1",
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@types/d3-geo": "^3.1.0",
    "@types/node": "22.18.1",
    "@types/react": "19.1.13",
    "@types/react-dom": "19.1.9",
    "@types/topojson-client": "^3.1.5",
    "playwright": "^1.56.1",
    "typescript": "5.9.2"
  }
}
